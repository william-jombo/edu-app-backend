<!-- <IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Don't handle OPTIONS here - let PHP handle it
    # Just allow .htaccess to work
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ $1 [L]
</IfModule>

# NOTE: CORS headers are handled in PHP (includes/cors.php) -->



 # .htaccess - Minimal config, CORS handled in PHP

<IfModule mod_rewrite.c>
    RewriteEngine On
</IfModule> 